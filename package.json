{
  "name": "agenthire",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/mcp-servers/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "eval": "turbo eval",
    "eval:report": "node packages/evals/reporters/cli.js"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "npm@10.0.0"
}
